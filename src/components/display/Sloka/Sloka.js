const Sloka = () => {
    return (
        <h1 style={styles.text}>Sloka text goes here</h1>
    );
};

const styles = {
    text: {
        padding: "10px 20px",
        fontSize: "16px",
        color: "red"
      },
};

export default Sloka;